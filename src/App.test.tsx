import { describe, it, expect } from 'vitest';

describe('App Test', () => {
  it('renders correctly', () => {
    // A simple test to assert that the test runner is configured correctly
    expect(true).toBe(true);
  });
});
